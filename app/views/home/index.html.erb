<div class="homepage">
  <div class='form-upload'>
    <%= render 'upload_form' %>
  </div>

  <% if @posts.load.any? %>
    <div class="posts" id="posts">
      <% @posts.each_with_index do |post, index| %>
        <%= render partial: 'post', locals: { post: post, user_reaction: @user_reactions[post.id], following: @following_ids.include?(post.user_id), image_loading: index.zero? ? "eager" : "lazy" } %>
      <% end %>
    </div>
    <%= render "feed_sentinel", posts: @posts %>
  <% else %>
    <div class="empty-state">
      <i class="fa fa-camera"></i>
      <p>No posts yet. Share your first photo above.</p>
    </div>
  <% end %>
</div>
