<% if posts.load.any? %>
  <div class="thumbnail-grid">
    <% posts.each do |post| %>
      <div class="wrapper">
        <%= link_to post_path(post) do %>
          <%= image_tag post.image.variant(resize_to_fill: [ 440, 440 ]), loading: "lazy", alt: "" %>
        <% end %>
      </div>
    <% end %>
  </div>

  <%= paginate posts %>
<% else %>
  <div class="empty-state">
    <i class="fa fa-camera"></i>
    <p>No posts yet.</p>
  </div>
<% end %>
