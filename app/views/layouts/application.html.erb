<!DOCTYPE html>
<html>
  <head>
    <title>Instagram</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <div id="realtime" data-turbo-permanent data-controller="inbox presence"></div>
    <% end %>

    <!--Navigation Bar-->
    <nav class="navbar navbar-light bg-light d-flex">
      <div class="container">
        <div class="logo">
          <a class="navbar-brand" href="<%= root_path %>">
            <%= fa_stacked_icon "camera inverse", base: "circle"%>
            Instagram
          </a>
        </div>
        <div class="m-auto">
          <%= form_with url: search_path, method: :get, local: true, class: 'd-flex search-form' do |form| %>
            <%= form.text_field :query, value: params[:query], class: 'form-control w-auto me-sm-2', placeholder: '#search' %>
          <% end %>
        </div>
        <div class="user-section">
          <a><i class="fa fa-compass"></i></a>
          <a><i class="fa fa-heart"></i></a>
          <% if user_signed_in? %>
            <% unread = current_user.unread_messages_count %>
            <a href="<%= conversations_path %>" class="messages-link">
              <i class="fa fa-paper-plane"></i>
              <span id="unread-badge" class="unread-badge <%= "is-hidden" if unread.zero? %>"><%= unread %></span>
            </a>
            <a href="<%= user_path(current_user)%>">
              <i class="fa fa-user"></i>
            </a>
          <% else %>
            <a href="<%= new_user_session_path%>">
              <i class="fa fa-sign-in"></i>
            </a>
          <% end %>
        </div>
      </div>
    </nav>

    <!--Main content-->
    <main class="container main-content">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == "alert" ? "danger" : "success" %>"><%= message %></div>
      <% end %>
      <%= yield %>
    </main>

    <!--Footer-->
    <%= render "layouts/footer" %>

    <%= turbo_frame_tag "post_modal" %>
  </body>
</html>
