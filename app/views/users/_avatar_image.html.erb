<% if user.avatar.attached? %>
  <%= image_tag user.avatar.variant(local_assigns.fetch(:variant, :thumb)),
                alt: user.username, loading: local_assigns[:loading] %>
<% else %>
  <span class="avatar-monogram"><%= user_monogram(user) %></span>
<% end %>
